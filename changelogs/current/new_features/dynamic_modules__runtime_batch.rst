dynamic modules: added bounded runtime snapshot batch reads for HTTP filters and dynamic clusters,
with Rust and Go SDK support. Each call reads one merged runtime snapshot and copies values into
module-owned storage.
