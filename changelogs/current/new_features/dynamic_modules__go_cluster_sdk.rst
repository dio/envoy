dynamic modules: added Go SDK cluster registration, host membership, main-thread scheduling,
request filter-state reads, and asynchronous host selection with cancellation and worker lifecycle
support.
